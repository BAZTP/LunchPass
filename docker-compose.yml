version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: lunchpass-postgres
    environment:
      POSTGRES_DB: lunchpassdb
      POSTGRES_USER: lunchpass
      POSTGRES_PASSWORD: lunchpass_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lunchpass -d lunchpassdb"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lunchpass-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/lunchpassdb
      SPRING_DATASOURCE_USERNAME: lunchpass
      SPRING_DATASOURCE_PASSWORD: lunchpass_password
      SPRING_PROFILES_ACTIVE: prod
      JWT_SECRET: 9a8b7c6d5e4f3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c8d7e6f5a4b3c2d1e0f9a8b
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: lunchpass-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
